import React from "react";
import Base from "../core/Base";

const Profile = () => {
  return (
  <Base title="Profile Page">
    <h1 className="text-primary">This is profile Page</h1>
  </Base>
  );
};

export default Profile;
